import * as chai from "chai";


describe('Movie Service',  () => {

    afterEach(async () => {
    })

    it('if block monthly limit', function (done) {

        //GIVEN
        let title = 'matrix';
        let newMovie = {title: title} ;
    });

});